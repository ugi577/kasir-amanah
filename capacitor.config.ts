import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'id.amanah.kasir',
  appName: 'Kasir Amanah',
  webDir: 'out',
  android: {
    allowMixedContent: false,
  },
};

export default config;
